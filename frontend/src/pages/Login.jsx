/**
 * SmartFit Login Page
 *
 * This page will eventually contain the user login form.
 *
 * The login form will communicate with the FastAPI backend
 * through the authentication API that we already implemented
 * during Milestone 2.
 */

function Login() {
  return (
    <div>
      {/* Page heading. */}
      <h1>Login</h1>

      {/* Temporary description until the login form is implemented. */}
      <p>
        Login to your SmartFit account.
      </p>
    </div>
  );
}

export default Login;